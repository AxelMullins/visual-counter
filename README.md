# Virtual Counter `(React + Vite)`

# Comandos:

- ## Instalar todas las librerías

  - `npm i`

- ## Correr el proyecto localmente

  - `npm run dev`

- ## Crear el build (crea la carpeta "dist")
  - `npm run build`
